# ── Core API ─────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────
supabase==2.7.4

# ── Web Crawling (Amazon + BigBasket) ────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2

# ── Playwright (Croma + Reliance Digital) ────────────────────
# After pip install, MUST also run: playwright install chromium
playwright>=1.44.0
playwright-stealth>=1.0.6

# ── NLP / ML Search Engine ───────────────────────────────────
scikit-learn==1.5.2
numpy==1.26.4

# ── Selenium (manual scraping tool only — scrapping.py) ──────
selenium==4.21.0

# ── Sentiment Analysis ───────────────────────────────────────
# Free, offline, no API key needed — works great for Indian English reviews
vaderSentiment>=3.3.2
